---
weight: 3
title: Sources
---

# Sources

You can choose to install re:Director from sources.  
Make sure to have a Java JDK 25 installed. The easiest way to do so is [SDKMAN!](https://sdkman.io/). A JDK from [Temurin](https://adoptium.net/temurin) is recommended.

## Clone the Repo

Use git to clone the repo from GitHub:

```bash
git clone https://github.com/re-Director/re-director.git
```

## Run

Change into the repository and run the following command:

```bash
./mvnw spring-boot:run
```